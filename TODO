Fix utils.py to do error catching if certain fields are not applicable
Make a button for dynamically adding plots
Make axes and figures globally acccessible and alterable
Allow overlaying plots by choosing one x and y axis based on all of the datasets that are added